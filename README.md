# ascii_video
```
pip install opencv-python numpy pytube
```


```
# Интерактивный режим
python ascii_video.py

# Прямой запуск YouTube видео
python ascii_video.py -s "https://youtu.be/dQw4w9WgXcQ" --color --charset blocks

# Запись в файл
python ascii_video.py -s input.mp4 -o output.txt
```
